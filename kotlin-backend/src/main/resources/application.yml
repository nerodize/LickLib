spring:
  application:
    name: kotlin-backend

  servlet:
    multipart:
      enabled: true
      max-file-size: 15MB
      max-request-size: 15MB

  datasource:
    url: jdbc:postgresql://localhost:5432/licklib

    username: admin
    password: p

  jpa:
    show-sql: true # Zeigt die generierten SQL-Statements in der Konsole an
    properties:
      hibernate.format_sql: true # Formatiert SQL lesbar

### Application Properties (MinIO)
bucket:
  endpoint: "http://localhost:9000"
  access-key: "minio_admin_user"
  secret-key: "supersecretpassword123"
  name: "songs"